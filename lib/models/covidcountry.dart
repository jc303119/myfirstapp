class Country{
  String country;
  String countryCode;
  String slug;
  int newConfirmed;
  int totalConfirmed;

  Country(this.country, this.countryCode, this.slug, this.newConfirmed,this.totalConfirmed);
}